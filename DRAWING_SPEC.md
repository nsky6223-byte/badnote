# 드로잉 엔진 상세 스펙

> 대상: Claude Code (VS Code 플러그인). 이 문서를 읽고 `src/canvas/` 아래 코드를 작성하면 됩니다.
> 처음 만들어보는 기능이므로, 각 항목에 "왜 필요한지"를 함께 적어둡니다.

---

## 1. 필요한 로직 / 기능 목록

### 1-1. 입력 처리
- **Pointer Events 통합 사용** — `pointerdown` / `pointermove` / `pointerup` / `pointercancel`만 사용하고 `mousedown`/`touchstart` 등은 사용하지 않는다. Pointer Events는 마우스·터치·펜을 하나의 API로 통합하고, `event.pressure`(필압), `event.tiltX`/`tiltY`(기울기), `event.pointerType`(`"pen" | "touch" | "mouse"`)을 함께 제공한다.
- **팜 리젝션(손바닥 오터치 방지)** — 패드에 손을 대고 펜으로 그릴 때 손바닥 터치가 그림을 망치지 않도록, `pointerType === "pen"`이 감지되면 동시 발생하는 `touch` 포인터를 무시. CSS `touch-action: none`을 캔버스에 필수 적용.
- **좌표 보정** — `canvas.getBoundingClientRect()`로 오프셋을 구하고, `window.devicePixelRatio`로 고해상도(레티나) 화면에서 선명하게 그려지도록 캔버스 실제 픽셀 크기와 CSS 크기를 분리해서 스케일링.

### 1-2. 스트로크(선) 렌더링
- **원시 포인터 좌표는 그대로 쓰지 않는다** — 사람 손 움직임은 미세하게 떨리기 때문에 raw point를 그대로 이으면 선이 각지고 지저분해 보인다. `perfect-freehand` 라이브러리로 포인트 배열 → 압력 반응형 매끄러운 외곽선(polygon)을 생성해 Canvas에 채워 그린다.
- **스트로크 데이터 모델**은 항상 벡터로 보관한다 (픽셀을 직접 조작하지 않음):
  ```ts
  type Point = { x: number; y: number; pressure: number; t: number };
  type Stroke = {
    id: string;
    tool: "pen" | "eraser";
    color: string;
    size: number;
    points: Point[];
  };
  ```
  이렇게 저장해야 나중에 "글리치" 효과(예: 색깔이 배신함, 글자가 재배열됨)가 픽셀이 아니라 **데이터**를 조작할 수 있다. 이번 스프린트에서는 글리치를 만들지 않지만, 이 구조를 미리 잡아두는 것이 핵심 결정이다.

### 1-3. 레이어 구조
- **배경 레이어**: 노트 종이 텍스처/줄무늬 (정적, 거의 다시 그리지 않음)
- **잉크 레이어**: 확정된(펜을 뗀) 스트로크들 — `pointerup` 시점에 커밋
- **활성 스트로크 레이어**: 지금 그리고 있는 선 — `pointermove`마다 다시 그려지는 유일한 레이어 (성능상 분리 필수)
- **UI 오버레이**: 툴바, 커서 프리뷰 등 (DOM, 캔버스 아님)

### 1-4. 도구
- **펜**: 색상 + 굵기 + 필압 반응
- **지우개**: 이번 스프린트는 **오브젝트 단위 지우개**로 구현 (스트로크와 지우개 경로가 겹치면 해당 스트로크를 통째로 삭제/분할). 픽셀 지우개(비트맵 지우기)보다 구현이 단순하고, 다음 스프린트의 "지우개가 도망다니는" 글리치를 붙이기도 쉽다.
- **색상 선택**: 기본 팔레트 5~8색 + 커스텀 컬러피커

### 1-5. Undo / Redo
- Canvas는 픽셀 기반이라 네이티브 undo가 없다. **스트로크 배열을 히스토리 스택으로 관리**하고, undo 시 스트로크 목록에서 마지막 항목을 제거한 뒤 전체를 다시 그린다(redraw). 스트로크 개수가 많아지면 매번 전체 redraw가 느려질 수 있어 1-6 성능 항목과 함께 고려.

### 1-6. 성능
- `pointermove`가 초당 수십~수백 번 발생할 수 있으므로, 렌더링은 **`requestAnimationFrame`으로 배칭** — 이벤트 핸들러에서는 좌표만 큐에 쌓고, rAF 콜백에서 한 번에 그린다.
- 스트로크 수가 많아지면(한 노트에 수백 개) 매번 전체 redraw 대신, 확정된 스트로크는 별도 캔버스에 누적해서 그리고 새 스트로크만 겹쳐 그리는 방식 고려 (2단계 캐시).

### 1-7. 로컬 저장
- IndexedDB에 현재 노트의 스트로크 배열(JSON)을 저장. 서버 없이도 새로고침/재방문 시 이전 그림 복원 가능.
- 저장 시점: 스트로크 커밋마다(디바운스 1~2초) + 페이지 이탈 시(`beforeunload`).

### 1-8. 반응형/배포
- 태블릿 세로/가로 모두 대응 (`resize` 이벤트에 캔버스 재계산).
- Vercel에 GitHub 리포지토리 연결 → `main` 브랜치 push마다 자동 배포, PR 브랜치마다 프리뷰 URL 생성 (팀원끼리 리뷰할 때 유용).

---

## 2. 우리가 정해야 하는 것 (팀 결정 사항)

AI가 기술적으로 구현은 할 수 있지만, **최종 선택은 제품/디자인 판단이라 팀이 정해야** 합니다.

| 결정 사항 | 선택지 | 추천(참고용) | 상태 |
|---|---|---|---|
| 지우개 방식 | 픽셀 지우개 vs 오브젝트 지우개 | 오브젝트 지우개 (다음 스프린트 글리치와 연동 쉬움) | ☐ 미정 |
| 캔버스 비율/배경 | 정사각형 / 세로 노트 / 무지·줄노트·모눈 | 세로 노트 + 줄노트 배경 (굿노트 느낌 극대화) | ☐ 미정 |
| 지원 기기 우선순위 | 아이패드+애플펜슬 우선 vs 범용(안드로이드 태블릿/마우스 폴백 동등 지원) | 아이패드 우선, 나머지는 폴백 | ☐ 미정 |
| 기본 컬러 팔레트 구성 | 몇 개 색? 브랜드 컬러 포함 여부 | 5~8색 + 커스텀 피커 | ☐ 미정 |
| 배포 플랫폼 | Vercel / Netlify / Cloudflare Pages | Vercel (GitHub 연동·프리뷰 배포 가장 매끄러움) | ☐ 미정 |
| GitHub 브랜치 전략 | 팀원별 역할 분담, PR 리뷰 규칙 | `main`(보호) + `feature/*`, PR 1개 이상 승인 후 머지 | ☐ 미정 |
| 로컬 저장 범위 | 노트 1개만 vs 여러 노트 관리 | 이번 스프린트는 1개 노트만 (범위 최소화) | ☐ 미정 |

> 이 표는 그대로 두고 팀 논의 후 "상태" 칸만 채워서 Claude Code에게 다시 전달하면, 확정된 값 기준으로 바로 구현을 진행할 수 있습니다.

## 3. AI(Claude Code)가 바로 담당할 수 있는 부분

위 표의 결정이 내려지면(또는 추천값 그대로 진행해도 좋다고 하면), 아래는 사람 판단 없이 AI가 구현까지 진행 가능한 영역입니다.

- [ ] Vite + React + TypeScript 프로젝트 스캐폴딩
- [ ] Pointer Events 기반 입력 처리 (`pointerInput.ts`)
- [ ] `perfect-freehand` 연동한 스트로크 렌더링 (`strokeEngine.ts`)
- [ ] 필압/기울기 → 선 굵기·불투명도 매핑
- [ ] 팜 리젝션, `touch-action: none` 적용
- [ ] 레이어 구조 구현 (배경/잉크/활성스트로크/UI)
- [ ] Undo/Redo 스택
- [ ] IndexedDB 저장/불러오기 (`idb` 라이브러리)
- [ ] rAF 기반 렌더링 배칭 (성능 최적화)
- [ ] 반응형 캔버스 리사이즈 처리
- [ ] 툴바 UI 컴포넌트 (펜/지우개/색상/굵기)
- [ ] Vercel 배포 설정 (`vercel.json`, GitHub 연동 가이드)
- [ ] 기본 테스트/린트 셋업

---

## 4. 4주 마일스톤 (초기 프로토타입)

| 주차 | 목표 |
|---|---|
| 1주차 | 프로젝트 셋업 (Vite+React+TS), GitHub 리포+Vercel 연동, 빈 캔버스에 마우스로 선 긋기까지 |
| 2주차 | Pointer Events 고도화 (필압/기울기/팜리젝션), `perfect-freehand` 적용, 색상·굵기 툴 |
| 3주차 | 지우개, Undo/Redo, 레이어 분리, IndexedDB 저장, 반응형 대응 |
| 4주차 | 성능 튜닝, 버그픽스, 실제 패드(아이패드 등)에서 테스트, 배포 마감 |

---

## 5. Claude Code에 던질 첫 프롬프트 예시

```
CLAUDE.md와 DRAWING_SPEC.md를 읽고, 1주차 목표부터 시작해줘.
Vite + React + TypeScript로 프로젝트를 셋업하고,
Pointer Events로 마우스 입력을 받아 캔버스에 선을 그리는
가장 기본적인 버전부터 만들어줘. perfect-freehand는
2주차에 붙일 예정이니 이번엔 raw point를 직선으로 이어서
"그려진다"는 것만 먼저 확인하자.
```
